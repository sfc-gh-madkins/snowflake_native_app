manifest_version: 1

version:
  name: hello_snowflake
  label: "v1.0"
  comment: "The first version of a Snowflake Native App"

artifacts:
  setup_script: consumer/setup.sql
  readme: README.md
  extension_code: true

configuration:
  log_level: debug
  trace_level: always
